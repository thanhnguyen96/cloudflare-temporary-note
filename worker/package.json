{
  "name": "note-24h-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/worker.ts --persist-to=.wrangler/state",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "aws4fetch": "^1.0.20",
    "@cloudflare/workers-types": "^4.20260404.0",
    "typescript": "^5.6.2",
    "wrangler": "^4.11.1"
  }
}
